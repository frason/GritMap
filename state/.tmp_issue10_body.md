depends_on: #3, #29

## Goal
Run the full matcher (issues #8+#29) against real Karoo FIT data (from issue #3's
fixtures/fit/ directory) to confirm it behaves correctly on real-world noise, not just
synthetic cases.

## Context
- Uses the real sample FIT files added for issue #3.
- If the client's sample files include multiple rides over the same physical stretch of
  road (per SPEC.md's "PR a hill climb" use case), define a segment from one ride
  (manually, via a throwaway script or the UI from issue #7 if it's done) and confirm the
  matcher finds the traversal(s) in the other real rides.
- If real fixtures don't include repeat rides over the same stretch, note this clearly in
  the output and instead validate: (a) the matcher runs end-to-end without crashing on
  real GPS noise/gaps, (b) a self-match (segment defined from ride A, matched back against
  ride A) is accepted with high confidence and near-100% coverage.
- Record any tuning needed to the 30m corridor / 90% coverage / 30s gap thresholds based on
  real GPS noise observed - but do NOT change the thresholds without flagging it in the
  output, since they're specified in docs/MVP.md.

## Done when
- At least one real-fixture self-match test passes (segment from ride A matches back
  against ride A with high confidence).
- Findings on real-world matcher behavior (false positives/negatives, threshold
  sensitivity) are written to the output file for later tuning reference.

## Output
Write a concise summary (<=40 lines) to your designated output file (the exact per-issue
path is given in your task prompt when the worker runs).

<!-- agent-planned -->
