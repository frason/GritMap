<!-- BUDGET:START (auto-generated by budget_check.sh; do not edit by hand) -->
## 5h rolling budget — updated 18:15
Spent: $4.31 / $10 soft cap  ██████░░░░░░░░ 43%
Runs: karen 9 · lead 3 · worker 5
<!-- BUDGET:END -->

# Project Status

_Updated by: lead_ &nbsp;·&nbsp; _When: 2026-08-13_

## Phase
build — see SPEC.md for the living requirements. Target date: **Oct 3, 2026** (fixed).

## What this project is
GritMap: Expo/React Native app to import Karoo FIT files, define a directed GPS segment,
auto-match every past/future attempt, and compare attempts on a distance-aligned chart —
replacing Strava segment matching (offline, no backend).

## Lanes
_Sized by the lead from the project plan; phase/sequence notes go under "Up next"._

| Lane     | Purpose                          | Last run | Now    |
|----------|----------------------------------|----------|--------|
| docs     | documentation, READMEs, comments | —        | idle   |
| tests    | writing / fixing tests           | —        | idle   |
| refactor | cleanups, small refactors        | —        | idle   |
| research | investigate & report (no edits)  | —        | idle   |
| verify   | audit done work (karen, Sonnet)  | —        | idle   |

## Lead
- Allowed windows: :00 and :30
- Inbox: 0 pending
- Last plan: 2026-08-13 (pass 3) — unblocked scaffold after 3 failed attempts; split #1
  into commit-first steps #24/#25; repointed #4/#17/#18 dependencies.

## Open questions for the client
- None open. #2 (sample Karoo FIT files) was answered and closed; fixtures live in
  `fixtures/fit/` and were used to validate the parser (see #3 comments / PR #16).

## Verification (claimed vs verified)
- #9 (matcher confidence/versioning/persistence) — claimed, awaiting karen in agent-review.
- #3, #8, #12 (FIT parser, matcher core, comparison-calc) — CLOSED via an external build
  track (detached from the normal worker/karen loop per their issue comments), landed via
  PRs #16/#21/#15. Worth a spot-check once #9/#10 land that their outputs are actually
  wired into the app, since they bypassed the usual verify loop.
- #1 (original monolithic scaffold) — FAILED karen verification 3x in a row, always the
  same failure mode: worker generated plausible files but ran out of turns before ever
  running `git commit`, so nothing landed. Closed as superseded; replaced with #24/#25
  (see below), which now commit-first before any install/verify step, specifically to
  survive a truncated run.
- Everything else below is unbuilt/unverified — no false claims on the board right now.

## Up next — dependency chain
- **#24** scaffold step 1 (agent-todo, no deps) — generate + **commit immediately** the
  bare Expo TS skeleton (package.json/app.json/tsconfig/entry/assets). No install yet.
- **#25** scaffold step 2 (backlog, depends #24) — npm install, tsc --noEmit verification,
  commit lockfile.
- **#17** expo-sqlite + DB init (backlog, depends #25).
- **#18** MapLibre dep + minimal MapView (backlog, depends #25).
- **#19** expo prebuild native projects + dev-setup docs (backlog, depends #17,#18).
- **#4** SQLite schema/migrations (backlog, depends #17).
- **#5** batch import + duplicates (backlog, depends #3(closed),#4).
- **#6** MapLibre track rendering (backlog, depends #4,#5,#19).
- **#7** segment definition, handles+scrubber (backlog, depends #4,#6).
- **#9** matcher confidence/versioning/persistence — **in agent-review now**.
- **#10** matcher validation on real fixtures (backlog, depends #3(closed),#9).
- **#11** diagnostic review screen + reevaluation (backlog, depends #7,#10).
- **#13** comparison chart UI (backlog, depends #6,#12(closed)).
- **#14** UX cleanup + perf + acceptance testing (backlog, depends #11,#13).

### Schedule risk (Oct 3, 2026 target, ~7 weeks out)
The scaffold (#1, now #24/#25) burned 3 worker attempts and multiple karen cycles without
landing a single commit — pure process failure (haiku running out of turns before the
mechanical `git commit` step), not a spec ambiguity. Rescoped to force a commit as the
*first* action in #24, before any install/verify work, specifically so a truncated run
still leaves something real behind. **Watch this**: if #24/#25 don't close within 1-2
cycles, the failure mode isn't the task shape anymore and needs a different fix (e.g. a
bigger turn budget for scaffold-class issues, or hand-authoring the skeleton instead of
routing it through create-expo-app). Everything else on the board (#4 through #14) is
gated behind this, so it's now the single highest-risk item for the Oct 3 date. Separately,
#3/#8/#12 landed via an external build track outside the normal karen loop — flag for a
spot-check once #10 (matcher validation) exercises them for real.

> Activity history is in `logs/activity.log`; cost per run is in `logs/usage.jsonl`.
> Keep this file SHORT — the PM reads it every turn, so brevity = lower token cost.
