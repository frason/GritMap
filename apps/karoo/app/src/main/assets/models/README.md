# Needle model asset

Place the pinned Needle 2 model binary in this directory for device builds. Model binaries
are deliberately ignored by Git. Record the upstream release/commit, filename, format,
license, byte size, and SHA-256 in `MODEL_PROVENANCE.md` before distributing an APK.

